name: Deployment Status

on:
  push:
    branches:
      - main

jobs:
  report:
    runs-on: ubuntu-latest

    steps:
      - name: Wait for webhook deployment
        run: |
          echo "⏳ Waiting for webhook deployment to complete..."
          sleep 10

      - name: Report deployment status
        run: |
          echo "✅ Webhook deployment triggered"
          echo "🚀 Test server should be running on latest commit"
          echo "📍 Commit: ${{ github.sha }}"
          echo "👤 Pushed by: ${{ github.actor }}"
          echo ""
          echo "The webhook listener deployed this commit to the test server."
          echo "Service 'nostr-hero-test' should have been restarted."
